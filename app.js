let numbs = [1,2,3,4];
function calSum(arr){
    let sum=0;
    for(i=0; i<arr.length; i++){i
        sum+=arr[];
    }
    return sum;
}
console.log(calSum(numbs));
 
numbs.sort((a,b)=> b-a);
console.log(numbs);


numbs.sort((a,b)=> (b-a));
console.log(Number);
